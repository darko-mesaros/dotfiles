# ─────────────────────────────────────────────────────────────
# Dotfiles .gitignore
# Focused on what a stow-managed dotfiles repo actually needs.
# NOTE: intentionally NOT ignoring lib/ build/ dist/ var/ etc. —
# those broad Python-template rules can silently hide real config.
# ─────────────────────────────────────────────────────────────

# ── Secrets (never commit) ──
secrets.zsh
*.secret
*.secrets
.env
.env.*
*.key
*.pem
id_rsa
id_ed25519
*_token
*_credentials

# ── Machine-local overrides ──
# Convention: source a *.local file if present, keep it out of git.
*.local
*.local.zsh
*.local.sh
*.local.toml

# Stow-managed Oatbar test launchers live under .local/bin; keep only these tracked.
!oatbar/.local/
!oatbar/.local/bin/
!oatbar/.local/bin/oatbar-0.3.1-test
!oatbar/.local/bin/oatbar-0.3.1-revert
!oatbar/.local/bin/oatbar-vpn-status

# ── Python bytecode (ranger / qtile configs) ──
__pycache__/
*.py[cod]
*$py.class
*.so

# ── Editor / tooling junk ──
*.swp
*.swo
*~
.DS_Store
tags
.direnv/

# ── Logs ──
*.log

# ── Local agent/tool config (not a shell dotfile, machine-local) ──
.kiro/
